import React from 'react';

const ImageContainer = ({ img, setDisplayImage }) => {
    const handleDisplayImage = img => {
        console.log(img);
    }
    return (
        <div>
            
        </div>
    );
};

export default ImageContainer;